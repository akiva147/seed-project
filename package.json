{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^8.53.0",
    "eslint-config-custom": "*",
    "prettier": "^3.0.3",
    "turbo": "^1.10.14"
  },
  "packageManager": "yarn@1.22.11",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "zod": "^3.22.2"
  }
}
